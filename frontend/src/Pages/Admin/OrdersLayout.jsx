import { Outlet } from "react-router";

/**
 * Layout for admin orders: renders list at /admin/orders and detail at /admin/orders/:orderId.
 */
const OrdersLayout = () => <Outlet />;

export default OrdersLayout;
